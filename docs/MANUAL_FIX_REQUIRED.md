# ⚠️ 중요: lib/sheetsClient.js 수동 수정 필요

파일 시스템 권한 문제로 자동 수정이 불가능합니다.
다음 파일을 텍스트 에디터로 수정해주세요:

## 📁 파일: lib/sheetsClient.js

### 수정할 줄: 16번째 줄

```javascript
// ❌ 현재 (잘못된 경로)
const keyFilePath = path.join(__dirname, 'google-credentials.json');

// ✅ 수정 후 (올바른 경로)
const keyFilePath = path.join(__dirname, '..', 'config', 'google-credentials.json');
```

## 수정 방법

### VS Code 사용 시:
1. `lib/sheetsClient.js` 파일 열기
2. Ctrl+G 누르고 "16" 입력하여 16번째 줄로 이동
3. 위의 코드로 수정
4. Ctrl+S로 저장

### 메모장 사용 시:
1. `lib/sheetsClient.js` 파일을 메모장으로 열기
2. Ctrl+F로 "google-credentials.json" 검색
3. 해당 줄 수정
4. 저장

## 수정 완료 후

```bash
node dx3bot.js
```

봇을 실행하여 정상 작동 확인!

---

**이 파일은 수정 완료 후 삭제하셔도 됩니다.**
